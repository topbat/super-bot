interface:
  display_name: "Super Bot"
  short_description: "Repo-specific patterns and workflows for super-bot"
  default_prompt: "Use the super-bot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true